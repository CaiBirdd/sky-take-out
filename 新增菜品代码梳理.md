# 新增菜品代码梳理

本文梳理当前项目里“新增菜品”这条业务链路，重点说明 Controller、Service、Mapper、XML 之间是怎么配合的。

## 1. 新增菜品涉及哪些文件

当前新增菜品主要涉及 7 个文件：

```text
sky-server/src/main/java/com/sky/controller/admin/DishController.java
sky-server/src/main/java/com/sky/service/DishService.java
sky-server/src/main/java/com/sky/service/impl/DishServiceImpl.java
sky-server/src/main/java/com/sky/mapper/DishMapper.java
sky-server/src/main/resources/mapper/DishMapper.xml
sky-server/src/main/java/com/sky/mapper/DishFlavorMapper.java
sky-server/src/main/resources/mapper/DishFlavorMapper.xml
```

还会用到几个已有类：

```text
DishDTO
Dish
DishFlavor
Result
AutoFill
OperationType
AutoFillAspect
```

## 2. 新增菜品的整体流程

新增菜品不是只插入一张表，而是可能涉及两张表：

```text
dish
保存菜品基本信息，例如名称、分类、价格、图片、描述、状态。

dish_flavor
保存菜品口味信息，例如甜度、温度、辣度等。
```

所以完整流程是：

```text
1. 前端提交新增菜品请求
2. DishController 接收 DishDTO
3. DishController 调用 DishService.saveWithFlavor
4. DishServiceImpl 把 DishDTO 拷贝成 Dish
5. DishMapper.insert 插入 dish 表
6. MyBatis 把数据库生成的 dish.id 回填到 Dish 对象中
7. Service 从 dish.getId() 拿到菜品 id
8. 遍历口味列表，给每个 DishFlavor 设置 dishId
9. DishFlavorMapper.insertBatch 批量插入 dish_flavor 表
10. Controller 返回 Result.success()
```

一句话总结：

```text
先保存菜品主表，拿到菜品 id，再保存口味从表。
```

## 3. 前端传来的数据 DishDTO

文件位置：

```text
sky-pojo/src/main/java/com/sky/dto/DishDTO.java
```

核心字段：

```text
private Long id;
private String name;
private Long categoryId;
private BigDecimal price;
private String image;
private String description;
private Integer status;
private List<DishFlavor> flavors = new ArrayList<>();
```

`DishDTO` 是前端传给后端的数据对象。

它不只是包含菜品基本信息，还包含口味列表：

```text
flavors
```

例如前端可能传：

```text
{
  "name": "宫保鸡丁",
  "categoryId": 1,
  "price": 28,
  "image": "http://localhost:8080/upload/images/xxx.png",
  "description": "经典川菜",
  "status": 1,
  "flavors": [
    {
      "name": "辣度",
      "value": "[\"不辣\",\"微辣\",\"中辣\"]"
    },
    {
      "name": "温度",
      "value": "[\"热\",\"常温\"]"
    }
  ]
}
```

这里要注意：

```text
DishDTO 适合接收前端请求。
Dish 适合对应 dish 数据库表。
DishFlavor 适合对应 dish_flavor 数据库表。
```

DTO 和数据库实体不是一回事，但字段有重叠。

## 4. DishController

文件位置：

```text
sky-server/src/main/java/com/sky/controller/admin/DishController.java
```

核心代码：

```text
@RestController
@RequestMapping("/admin/dish")
@Api(tags = "菜品相关接口")
@Slf4j
public class DishController {

    @Autowired
    private DishService dishService;

    @PostMapping
    @ApiOperation("新增菜品")
    public Result save(@RequestBody DishDTO dishDTO) {
        log.info("新增菜品：{}", dishDTO);
        dishService.saveWithFlavor(dishDTO);
        return Result.success();
    }
}
```

这个 Controller 提供新增菜品接口：

```text
POST /admin/dish
```

因为类上有：

```text
@RequestMapping("/admin/dish")
```

方法上有：

```text
@PostMapping
```

所以完整路径就是：

```text
POST /admin/dish
```

### 4.1 @RequestBody 的作用

```text
public Result save(@RequestBody DishDTO dishDTO)
```

`@RequestBody` 表示从请求体中读取 JSON，并转换成 `DishDTO` 对象。

如果前端传 JSON：

```text
{
  "name": "宫保鸡丁",
  "categoryId": 1,
  "price": 28,
  "status": 1
}
```

Spring MVC 会帮你转成：

```text
DishDTO dishDTO
```

然后 Controller 不直接操作数据库，而是调用 Service：

```text
dishService.saveWithFlavor(dishDTO);
```

这是典型分层：

```text
Controller 负责接请求、返回响应
Service 负责业务逻辑
Mapper 负责数据库操作
```

## 5. DishService

文件位置：

```text
sky-server/src/main/java/com/sky/service/DishService.java
```

核心代码：

```text
public interface DishService {

    void saveWithFlavor(DishDTO dishDTO);
}
```

这是 Service 接口。

它定义了一个方法：

```text
saveWithFlavor
```

从名字就能看出，它不是只保存菜品，还会保存菜品口味。

为什么要先定义接口？

常见原因：

```text
1. Controller 依赖接口，而不是具体实现类
2. 后续实现可以替换
3. 符合项目里已有的分层习惯
```

## 6. DishServiceImpl

文件位置：

```text
sky-server/src/main/java/com/sky/service/impl/DishServiceImpl.java
```

核心代码：

```text
@Service
public class DishServiceImpl implements DishService {

    @Autowired
    private DishMapper dishMapper;

    @Autowired
    private DishFlavorMapper dishFlavorMapper;

    @Override
    public void saveWithFlavor(DishDTO dishDTO) {
        Dish dish = new Dish();
        BeanUtils.copyProperties(dishDTO, dish);

        dishMapper.insert(dish);

        Long dishId = dish.getId();

        List<DishFlavor> flavors = dishDTO.getFlavors();
        if (flavors != null && flavors.size() > 0) {
            flavors.forEach(dishFlavor -> {
                dishFlavor.setDishId(dishId);
            });
            dishFlavorMapper.insertBatch(flavors);
        }
    }
}
```

这个类是真正处理新增菜品业务的地方。

### 6.1 注入两个 Mapper

```text
private DishMapper dishMapper;
private DishFlavorMapper dishFlavorMapper;
```

为什么需要两个 Mapper？

因为新增菜品要操作两张表：

```text
dishMapper
负责操作 dish 表。

dishFlavorMapper
负责操作 dish_flavor 表。
```

### 6.2 DTO 转实体 Dish

```text
Dish dish = new Dish();
BeanUtils.copyProperties(dishDTO, dish);
```

`DishDTO` 是前端请求对象，`Dish` 是菜品实体对象。

这句会把同名属性复制过去，例如：

```text
name
categoryId
price
image
description
status
```

不会复制 `flavors` 到 `Dish`，因为 `Dish` 类里没有 `flavors` 字段。

这正好符合需求：

```text
Dish 只保存 dish 表字段。
flavors 单独保存到 dish_flavor 表。
```

### 6.3 插入菜品主表

```text
dishMapper.insert(dish);
```

这一步插入的是 `dish` 表。

这里有一个很关键的点：

```text
dishMapper.insert(dish) 执行完后，dish.getId() 能拿到数据库生成的主键 id。
```

这是因为 XML 中配置了：

```text
useGeneratedKeys="true"
keyProperty="id"
```

后面会详细讲。

### 6.4 获取菜品 id

```text
Long dishId = dish.getId();
```

为什么要拿这个 id？

因为 `dish_flavor` 表中的每条口味数据都要知道它属于哪个菜品。

也就是每条口味记录都需要：

```text
dish_id
```

例如：

```text
dish 表：
id = 10, name = 宫保鸡丁

dish_flavor 表：
dish_id = 10, name = 辣度
dish_id = 10, name = 温度
```

这样才能通过 `dish_id` 建立关系。

### 6.5 给每个口味设置 dishId

```text
List<DishFlavor> flavors = dishDTO.getFlavors();
if (flavors != null && flavors.size() > 0) {
    flavors.forEach(dishFlavor -> {
        dishFlavor.setDishId(dishId);
    });
    dishFlavorMapper.insertBatch(flavors);
}
```

前端传来的口味数据通常没有 `dishId`。

因为前端新增菜品时，菜品还没插入数据库，还不知道菜品 id。

所以流程必须是：

```text
1. 先插入 dish 表
2. 数据库生成 dish.id
3. 后端拿到 dish.id
4. 给每个 DishFlavor 设置 dishId
5. 再插入 dish_flavor 表
```

如果不设置 `dishId`，口味数据即使插入了，也无法知道属于哪个菜品。

## 7. DishMapper

文件位置：

```text
sky-server/src/main/java/com/sky/mapper/DishMapper.java
```

核心代码：

```text
@Mapper
public interface DishMapper {

    @Select("select count(id) from dish where category_id = #{categoryId}")
    Integer countByCategoryId(Long categoryId);

    @AutoFill(value = OperationType.INSERT)
    void insert(Dish dish);
}
```

`DishMapper` 负责操作 `dish` 表。

### 7.1 countByCategoryId

```text
Integer countByCategoryId(Long categoryId);
```

这个方法不是新增菜品的核心，但和分类删除有关。

它用于查询某个分类下有多少菜品：

```text
select count(id) from dish where category_id = #{categoryId}
```

如果某个分类下已经有关联菜品，删除分类时就应该禁止删除。

### 7.2 insert 方法

```text
@AutoFill(value = OperationType.INSERT)
void insert(Dish dish);
```

这是新增菜品主表的方法。

这里有两个关键点。

第一个：

```text
@AutoFill(value = OperationType.INSERT)
```

表示这个方法执行前，会触发 `AutoFillAspect`。

切面会自动给 `Dish` 填充：

```text
createTime
createUser
updateTime
updateUser
```

所以 Service 中不需要手动写：

```text
dish.setCreateTime(...)
dish.setCreateUser(...)
dish.setUpdateTime(...)
dish.setUpdateUser(...)
```

第二个：

```text
void insert(Dish dish);
```

这个方法的 SQL 没写在注解里，而是写在 `DishMapper.xml` 里。

## 8. DishMapper.xml

文件位置：

```text
sky-server/src/main/resources/mapper/DishMapper.xml
```

核心代码：

```text
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, create_time, update_time, create_user, update_user, status)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{status})
    </insert>
</mapper>
```

### 8.1 namespace

```text
<mapper namespace="com.sky.mapper.DishMapper">
```

表示这个 XML 对应的是：

```text
com.sky.mapper.DishMapper
```

MyBatis 会根据 namespace 和方法名找到对应 SQL。

### 8.2 insert id

```text
<insert id="insert">
```

这里的 `id="insert"` 对应 Java 接口里的：

```text
void insert(Dish dish);
```

所以调用：

```text
dishMapper.insert(dish)
```

MyBatis 就会执行 XML 里的这个 `<insert>` SQL。

### 8.3 useGeneratedKeys 和 keyProperty

```text
useGeneratedKeys="true" keyProperty="id"
```

这两个配置非常重要。

它们的作用是：

```text
插入数据后，把数据库自动生成的主键 id 回填到 dish 对象的 id 属性中。
```

假设插入前：

```text
dish.id = null
```

执行：

```text
dishMapper.insert(dish)
```

数据库生成主键：

```text
id = 10
```

执行后：

```text
dish.getId() = 10
```

这就是为什么 Service 后面能写：

```text
Long dishId = dish.getId();
```

如果没有这个配置，`dish.getId()` 很可能还是 `null`，后面的口味数据就没法正确关联菜品。

### 8.4 SQL 字段映射

SQL：

```text
insert into dish (name, category_id, price, image, description, create_time, update_time, create_user, update_user, status)
values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser}, #{status})
```

左边是数据库字段：

```text
category_id
create_time
update_time
create_user
update_user
```

右边是 Java 对象属性：

```text
categoryId
createTime
updateTime
createUser
updateUser
```

因为项目里 MyBatis 开启了驼峰映射：

```text
map-underscore-to-camel-case: true
```

所以数据库下划线字段和 Java 驼峰属性可以自然对应。

不过在 XML 里，`#{categoryId}` 这些写的仍然是 Java 对象属性名，不是数据库字段名。

## 9. DishFlavorMapper

文件位置：

```text
sky-server/src/main/java/com/sky/mapper/DishFlavorMapper.java
```

核心代码：

```text
@Mapper
public interface DishFlavorMapper {

    void insertBatch(List<DishFlavor> flavors);
}
```

这个 Mapper 负责操作菜品口味表：

```text
dish_flavor
```

新增菜品时，一个菜品可能有多个口味，所以这里用批量插入：

```text
insertBatch
```

而不是一条一条插入。

## 10. DishFlavorMapper.xml

文件位置：

```text
sky-server/src/main/resources/mapper/DishFlavorMapper.xml
```

核心代码：

```text
<mapper namespace="com.sky.mapper.DishFlavorMapper">
    <insert id="insertBatch">
        insert into dish_flavor (dish_id, name, value) VALUES
        <foreach collection="flavors" item="df" separator=",">
            (#{df.dishId}, #{df.name}, #{df.value})
        </foreach>
    </insert>
</mapper>
```

### 10.1 collection="flavors"

Java 方法参数是：

```text
void insertBatch(List<DishFlavor> flavors);
```

XML 中：

```text
collection="flavors"
```

表示遍历这个列表。

注意：如果没有额外加 `@Param`，MyBatis 对单个 List 参数通常也可以用 `list` 或 `collection`。当前这里写 `flavors` 在你的项目里如果能正常执行，说明参数名被 MyBatis 正确识别了；如果后续报参数找不到，可以改成给接口参数加：

```text
@Param("flavors")
```

例如：

```text
void insertBatch(@Param("flavors") List<DishFlavor> flavors);
```

### 10.2 item="df"

```text
item="df"
```

表示每次循环时，当前这一个口味对象叫 `df`。

所以可以写：

```text
#{df.dishId}
#{df.name}
#{df.value}
```

### 10.3 separator=","

```text
separator=","
```

表示每组 values 之间用逗号隔开。

最终生成的 SQL 类似：

```text
insert into dish_flavor (dish_id, name, value) VALUES
(10, '辣度', '["不辣","微辣","中辣"]'),
(10, '温度', '["热","常温"]')
```

这就是批量插入。

## 11. AutoFill 在新增菜品中的作用

`DishMapper.insert` 上有：

```text
@AutoFill(value = OperationType.INSERT)
```

所以调用：

```text
dishMapper.insert(dish)
```

之前会先进入：

```text
AutoFillAspect.autoFill(...)
```

它会自动给 `dish` 设置：

```text
createTime
createUser
updateTime
updateUser
```

这些字段最终会被插入到 SQL：

```text
#{createTime}
#{updateTime}
#{createUser}
#{updateUser}
```

所以新增菜品流程中，公共字段不是在 `DishServiceImpl` 里手动 set 的，而是通过 AOP 自动填充的。

## 12. 数据库关系

`dish` 和 `dish_flavor` 是一对多关系：

```text
一个菜品可以有多个口味。
```

例如：

```text
dish
id    name
10    宫保鸡丁

dish_flavor
id    dish_id    name
1     10         辣度
2     10         温度
```

这里的关键字段是：

```text
dish_flavor.dish_id
```

它指向：

```text
dish.id
```

所以新增时必须先插 `dish`，拿到 `dish.id`，再插 `dish_flavor`。

## 13. 为什么不能先插入口味

因为新增菜品时，数据库还没有生成菜品 id。

如果先插入口味，就不知道：

```text
dish_flavor.dish_id 应该填多少
```

所以顺序必须是：

```text
先插主表 dish
再插从表 dish_flavor
```

这也是很多一对多新增业务的常见写法。

## 14. 当前代码的一个注意点：事务

当前 `DishServiceImpl.saveWithFlavor` 没有看到 `@Transactional`。

这个方法里有两次数据库操作：

```text
1. dishMapper.insert(dish)
2. dishFlavorMapper.insertBatch(flavors)
```

如果第一步成功，第二步失败，就可能出现：

```text
dish 表有菜品
dish_flavor 表没有对应口味
```

为了保证一致性，通常建议在 Service 方法上加事务：

```text
@Transactional
public void saveWithFlavor(DishDTO dishDTO) {
    ...
}
```

这样如果批量插入口味失败，新增菜品主表也会回滚。

当前项目的启动类已经有：

```text
@EnableTransactionManagement
```

说明项目支持注解事务。后续学习到事务时，可以把这个点补上。

## 15. 当前截图里的 No mapping for GET /admin/dish/page

截图控制台里有一行：

```text
No mapping for GET /admin/dish/page
```

这表示前端请求了：

```text
GET /admin/dish/page
```

但是当前 `DishController` 里还只有新增接口：

```text
POST /admin/dish
```

还没有写菜品分页查询接口：

```text
GET /admin/dish/page
```

所以这个提示和新增菜品接口本身不是一回事。它说明“菜品分页查询”还没实现。

## 16. 新增菜品主线记忆法

可以按这条线记：

```text
DishController.save
-> DishService.saveWithFlavor
-> BeanUtils.copyProperties
-> DishMapper.insert
-> AutoFillAspect 自动填充公共字段
-> useGeneratedKeys 回填 dish.id
-> 遍历 flavors 设置 dishId
-> DishFlavorMapper.insertBatch
-> foreach 拼接批量插入 SQL
```

如果只记一句话：

```text
新增菜品 = 插入 dish 主表 + 获取主键 id + 批量插入 dish_flavor 从表。
```
