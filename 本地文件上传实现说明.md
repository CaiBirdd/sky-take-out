# 本地文件上传实现说明

本文说明本项目新增的本地文件上传实现。这个实现用于替代教程里的阿里云 OSS 上传，适合学习阶段使用。

## 1. 整体思路

前端上传图片后，后端不再把文件传到阿里云 OSS，而是保存到本地目录。

整体流程：

```text
前端选择图片
-> 调用后端上传接口
-> 后端把图片保存到本地 upload/images 目录
-> 后端生成一个可访问的图片 URL
-> 前端拿到 URL 后回显图片
-> 后续保存菜品、套餐时，把这个 URL 保存到数据库
```

这和 OSS 的核心流程是类似的：前端最终都拿到一个图片 URL。区别只是文件实际存储位置不同。

## 2. 新增文件总览

本次新增了这些文件：

```text
sky-common/src/main/java/com/sky/properties/LocalUploadProperties.java
sky-server/src/main/java/com/sky/config/LocalUploadConfiguration.java
sky-server/src/main/java/com/sky/controller/admin/CommonController.java
sky-server/src/main/java/com/sky/controller/common/LocalFileController.java
upload/.gitignore
```

它们分别负责：

```text
LocalUploadProperties
保存本地上传相关配置。

LocalUploadConfiguration
给上传配置补默认值。

CommonController
接收前端上传的文件，并保存到本地。

LocalFileController
根据图片 URL 读取本地文件，并返回给浏览器。

upload/.gitignore
防止本地测试上传的图片被提交到 Git。
```

## 3. LocalUploadProperties

文件位置：

```text
sky-common/src/main/java/com/sky/properties/LocalUploadProperties.java
```

源码核心：

```java
@Component
@ConfigurationProperties(prefix = "sky.local-upload")
@Data
public class LocalUploadProperties {

    private String path;

    private String urlPrefix;
}
```

这个类是配置属性类。

`@Component` 表示它会被 Spring 扫描并创建成 Bean。

`@ConfigurationProperties(prefix = "sky.local-upload")` 表示它可以读取配置文件中以 `sky.local-upload` 开头的配置。

如果以后想把配置写到 `application.yml`，可以这样写：

```yaml
sky:
  local-upload:
    path: C:/sky-take-out/upload/images
    url-prefix: http://localhost:8080/upload/images
```

其中：

```text
path
表示文件保存到本地哪个目录。

urlPrefix
表示返回给前端的图片访问地址前缀。
```

例如：

```text
path = C:/sky-take-out/upload/images
urlPrefix = http://localhost:8080/upload/images
```

上传后的文件名是：

```text
abc.png
```

那么返回给前端的 URL 就是：

```text
http://localhost:8080/upload/images/abc.png
```

## 4. LocalUploadConfiguration

文件位置：

```text
sky-server/src/main/java/com/sky/config/LocalUploadConfiguration.java
```

这个类负责给 `LocalUploadProperties` 补默认值。

源码核心：

```java
@Configuration
public class LocalUploadConfiguration {

    private static final String DEFAULT_URL_PREFIX = "http://localhost:8080/upload/images";

    @Autowired
    private LocalUploadProperties localUploadProperties;

    @PostConstruct
    public void init() {
        if (localUploadProperties.getPath() == null || localUploadProperties.getPath().isEmpty()) {
            localUploadProperties.setPath(Paths.get(System.getProperty("user.dir"), "upload", "images").toString());
        }
        if (localUploadProperties.getUrlPrefix() == null || localUploadProperties.getUrlPrefix().isEmpty()) {
            localUploadProperties.setUrlPrefix(DEFAULT_URL_PREFIX);
        }
    }
}
```

`@Configuration` 表示这是一个配置类。

`@PostConstruct` 表示 Spring 创建完这个 Bean，并完成依赖注入之后，自动执行 `init()` 方法。

这里做了两件事：

```text
1. 如果没有配置 path，就默认保存到项目运行目录下的 upload/images
2. 如果没有配置 urlPrefix，就默认使用 http://localhost:8080/upload/images
```

这一句：

```java
System.getProperty("user.dir")
```

表示获取当前 Java 程序的运行目录。

如果你是在项目根目录 `C:\sky-take-out` 启动项目，那么默认上传目录就是：

```text
C:\sky-take-out\upload\images
```

## 5. CommonController

文件位置：

```text
sky-server/src/main/java/com/sky/controller/admin/CommonController.java
```

这个类是上传接口。

接口路径：

```text
POST /admin/common/upload
```

源码核心：

```java
@PostMapping("/upload")
public Result<String> upload(MultipartFile file) {
    ...
}
```

`MultipartFile` 是 Spring MVC 提供的文件上传对象。

前端以表单方式上传文件时，后端就可以用它接收文件。

这个方法内部主要做了几步。

### 5.1 判断文件是否为空

```java
if (file == null || file.isEmpty()) {
    return Result.error(MessageConstant.UPLOAD_FAILED);
}
```

如果前端没有传文件，或者文件内容为空，直接返回上传失败。

### 5.2 获取原文件后缀

```java
String originalFilename = file.getOriginalFilename();
String suffix = "";
if (originalFilename != null && originalFilename.contains(".")) {
    suffix = originalFilename.substring(originalFilename.lastIndexOf("."));
}
```

例如原文件名是：

```text
food.png
```

那么后缀就是：

```text
.png
```

保留后缀的目的是让图片文件仍然有正确的文件类型。

### 5.3 生成新文件名

```java
String fileName = UUID.randomUUID() + suffix;
```

这样可以避免不同用户上传同名文件时互相覆盖。

例如生成：

```text
57e3a21c-aad0-4b5c-acf6-197791c3c23a.png
```

### 5.4 创建上传目录

```java
File uploadDir = new File(localUploadProperties.getPath());
if (!uploadDir.exists()) {
    uploadDir.mkdirs();
}
```

如果 `upload/images` 目录不存在，就自动创建。

### 5.5 保存文件

```java
file.transferTo(new File(uploadDir, fileName));
```

这句是真正把上传文件保存到本地磁盘。

例如保存成：

```text
C:\sky-take-out\upload\images\57e3a21c-aad0-4b5c-acf6-197791c3c23a.png
```

### 5.6 返回访问 URL

```java
String url = localUploadProperties.getUrlPrefix() + "/" + fileName;
return Result.success(url);
```

返回给前端：

```json
{
  "code": 1,
  "data": "http://localhost:8080/upload/images/57e3a21c-aad0-4b5c-acf6-197791c3c23a.png"
}
```

前端拿 `data` 就可以回显图片。

## 6. LocalFileController

文件位置：

```text
sky-server/src/main/java/com/sky/controller/common/LocalFileController.java
```

这个类负责让浏览器能够访问本地图片。

接口路径：

```text
GET /upload/images/{fileName}
```

例如浏览器访问：

```text
http://localhost:8080/upload/images/57e3a21c-aad0-4b5c-acf6-197791c3c23a.png
```

会进入：

```java
@GetMapping("/{fileName}")
public ResponseEntity<Resource> getImage(@PathVariable String fileName) throws IOException {
    ...
}
```

### 6.1 防止路径穿越

```java
if (fileName.contains("/") || fileName.contains("\\")) {
    return ResponseEntity.notFound().build();
}
```

这里只允许普通文件名，不允许带路径分隔符。

比如正常：

```text
abc.png
```

不允许：

```text
../application.yml
```

这是为了防止别人通过 URL 访问上传目录之外的文件。

### 6.2 拼接并校验文件路径

```java
Path uploadPath = Paths.get(localUploadProperties.getPath()).toAbsolutePath().normalize();
Path filePath = uploadPath.resolve(fileName).normalize();
if (!filePath.startsWith(uploadPath) || !Files.isRegularFile(filePath)) {
    return ResponseEntity.notFound().build();
}
```

这里做了两个校验：

```text
1. 最终文件路径必须仍然在上传目录下
2. 这个路径必须是一个真实存在的普通文件
```

如果文件不存在，就返回 404。

### 6.3 设置响应类型并返回文件

```java
String contentType = Files.probeContentType(filePath);
MediaType mediaType = contentType == null ? MediaType.APPLICATION_OCTET_STREAM : MediaType.parseMediaType(contentType);

Resource resource = new FileSystemResource(filePath);
return ResponseEntity.ok()
        .contentType(mediaType)
        .body(resource);
```

这段代码会读取文件类型，比如：

```text
image/png
image/jpeg
```

然后把文件内容返回给浏览器。

浏览器收到图片内容后，就能正常显示。

## 7. upload/.gitignore

文件位置：

```text
upload/.gitignore
```

内容：

```gitignore
*
!.gitignore
```

意思是：

```text
忽略 upload 目录下所有文件
但保留 upload/.gitignore 这个文件本身
```

这样做的原因是：上传图片属于本地运行数据，不应该提交到 Git。

如果不忽略，测试上传几张图片后，Git 会显示很多未跟踪文件。

## 8. 一次完整请求示例

假设前端上传一张图片：

```text
food.png
```

请求：

```text
POST http://localhost:8080/admin/common/upload
form-data:
  file = food.png
```

后端保存成：

```text
C:\sky-take-out\upload\images\57e3a21c-aad0-4b5c-acf6-197791c3c23a.png
```

后端返回：

```json
{
  "code": 1,
  "data": "http://localhost:8080/upload/images/57e3a21c-aad0-4b5c-acf6-197791c3c23a.png"
}
```

前端用返回的 URL 访问图片：

```text
GET http://localhost:8080/upload/images/57e3a21c-aad0-4b5c-acf6-197791c3c23a.png
```

后端从本地目录读取文件并返回图片内容。

## 9. 和阿里云 OSS 的区别

本地上传：

```text
文件保存在你自己的电脑上
配置简单
适合学习和本地开发
换电脑或部署服务器后，图片不会自动同步
```

阿里云 OSS：

```text
文件保存在云端对象存储
前端拿到的是公网可访问 URL
适合生产环境
需要配置 bucket、accessKey、权限、域名等
```

学习阶段用本地上传完全够用，因为你主要学习的是：

```text
如何接收文件
如何保存文件
如何返回 URL
如何让前端回显
如何把图片地址保存到数据库
```

这些流程和 OSS 版本是一致的。

## 10. 一句话总结

这套实现的核心是：

```text
CommonController 负责上传并保存文件；
LocalFileController 负责根据 URL 返回本地图片；
LocalUploadProperties 和 LocalUploadConfiguration 负责提供保存目录和访问前缀；
upload/.gitignore 负责避免测试图片进入 Git。
```
