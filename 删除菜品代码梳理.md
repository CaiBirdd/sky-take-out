# 删除菜品代码梳理

本文梳理当前项目里“删除菜品”的新增代码，重点说明 Controller、Service、Mapper、XML 之间的调用关系，以及为什么删除前要做校验。

## 1. 删除菜品涉及哪些文件

当前删除菜品主要涉及这些文件：

```text
sky-server/src/main/java/com/sky/controller/admin/DishController.java
sky-server/src/main/java/com/sky/service/DishService.java
sky-server/src/main/java/com/sky/service/impl/DishServiceImpl.java
sky-server/src/main/java/com/sky/mapper/DishMapper.java
sky-server/src/main/java/com/sky/mapper/DishFlavorMapper.java
sky-server/src/main/java/com/sky/mapper/SetmealDishMapper.java
sky-server/src/main/resources/mapper/SetmealDishMapper.xml
```

还会用到这些已有类：

```text
StatusConstant
MessageConstant
DeletionNotAllowedException
Result
```

## 2. 删除菜品的整体流程

删除菜品不是直接删除 `dish` 表一条数据就结束。

因为菜品可能还关联：

```text
1. 菜品口味表 dish_flavor
2. 套餐菜品关系表 setmeal_dish
```

所以删除前要先判断能不能删。

整体流程：

```text
1. 前端发送 DELETE /admin/dish?ids=1,2,3
2. DishController 接收 ids
3. Controller 调用 dishService.deleteBatch(ids)
4. Service 遍历 ids，检查是否存在起售中的菜品
5. 如果有起售中的菜品，抛出异常，不允许删除
6. Service 查询这些菜品是否被套餐关联
7. 如果被套餐关联，抛出异常，不允许删除
8. 校验通过后，删除 dish 表中的菜品数据
9. 删除 dish_flavor 表中的口味数据
10. Controller 返回 Result.success()
```

一句话总结：

```text
删除菜品 = 先校验能不能删，再删除菜品主表和口味表数据。
```

## 3. DishController 删除接口

文件位置：

```text
sky-server/src/main/java/com/sky/controller/admin/DishController.java
```

核心代码：

```text
@DeleteMapping
@ApiOperation("菜品批量删除")
public Result delete(@RequestParam List<Long> ids) {
    log.info("菜品批量删除：{}", ids);
    dishService.deleteBatch(ids);
    return Result.success();
}
```

类上有：

```text
@RequestMapping("/admin/dish")
```

方法上有：

```text
@DeleteMapping
```

所以完整接口是：

```text
DELETE /admin/dish
```

前端请求一般是：

```text
DELETE /admin/dish?ids=1,2,3
```

或者：

```text
DELETE /admin/dish?ids=1&ids=2&ids=3
```

Spring MVC 会把请求参数绑定成：

```text
List<Long> ids
```

也就是：

```text
[1, 2, 3]
```

Controller 不直接写删除 SQL，而是调用：

```text
dishService.deleteBatch(ids);
```

这符合项目分层习惯：

```text
Controller 负责接收请求
Service 负责业务校验和业务流程
Mapper 负责数据库操作
```

## 4. DishService 接口

文件位置：

```text
sky-server/src/main/java/com/sky/service/DishService.java
```

新增方法：

```text
void deleteBatch(List<Long> ids);
```

这个方法名表示：

```text
批量删除菜品
```

为什么参数是 `List<Long>`？

因为前端可能一次删除多个菜品。

例如：

```text
ids = [1, 2, 3]
```

Service 接口只是定义能力，真正实现逻辑在 `DishServiceImpl`。

## 5. DishServiceImpl 删除逻辑

文件位置：

```text
sky-server/src/main/java/com/sky/service/impl/DishServiceImpl.java
```

核心代码：

```text
@Autowired
private SetmealDishMapper setmealDishMapper;

@Transactional
@Override
public void deleteBatch(List<Long> ids) {
    for (Long id : ids) {
        Dish dish = dishMapper.getById(id);
        if (dish.getStatus() == StatusConstant.ENABLE) {
            throw new DeletionNotAllowedException(MessageConstant.DISH_ON_SALE);
        }
    }

    List<Long> setmealIds = setmealDishMapper.getSetmealIdsByDishIds(ids);
    if (setmealIds != null && setmealIds.size() > 0) {
        throw new DeletionNotAllowedException(MessageConstant.DISH_BE_RELATED_BY_SETMEAL);
    }

    for (Long id : ids) {
        dishMapper.deleteById(id);
        dishFlavorMapper.deleteByDishId(id);
    }
}
```

这个方法是删除菜品的核心。

## 6. 为什么要加 @Transactional

方法上有：

```text
@Transactional
```

表示这个删除流程要放在一个事务里。

因为删除菜品会涉及多次数据库操作：

```text
1. 查询菜品状态
2. 查询套餐关联
3. 删除 dish 表
4. 删除 dish_flavor 表
```

如果没有事务，可能出现部分成功、部分失败。

例如：

```text
dish 表删除成功
dish_flavor 表删除失败
```

这样数据库就会出现脏数据。

加了事务后，只要中途抛出运行时异常，前面已经执行的数据库操作会回滚。

## 7. 第一步校验：起售中的菜品不能删除

代码：

```text
for (Long id : ids) {
    Dish dish = dishMapper.getById(id);
    if (dish.getStatus() == StatusConstant.ENABLE) {
        throw new DeletionNotAllowedException(MessageConstant.DISH_ON_SALE);
    }
}
```

这里逐个查询菜品。

调用：

```text
dishMapper.getById(id)
```

拿到菜品对象。

然后判断：

```text
dish.getStatus() == StatusConstant.ENABLE
```

`StatusConstant.ENABLE` 在项目里通常表示：

```text
1
```

也就是起售状态。

如果菜品正在起售，就不能删除，于是抛出：

```text
DeletionNotAllowedException
```

异常消息是：

```text
MessageConstant.DISH_ON_SALE
```

也就是：

```text
起售中的菜品不能删除
```

为什么要禁止删除起售菜品？

因为起售菜品可能正在被用户浏览、下单。如果直接删除，会影响业务数据完整性。

## 8. DishMapper.getById

文件位置：

```text
sky-server/src/main/java/com/sky/mapper/DishMapper.java
```

代码：

```text
@Select("select  * from dish where id = #{id}")
Dish getById(Long id);
```

作用是根据菜品 id 查询菜品信息。

这里主要是为了拿到：

```text
status
```

然后判断菜品是否正在起售。

## 9. 第二步校验：被套餐关联的菜品不能删除

代码：

```text
List<Long> setmealIds = setmealDishMapper.getSetmealIdsByDishIds(ids);
if (setmealIds != null && setmealIds.size() > 0) {
    throw new DeletionNotAllowedException(MessageConstant.DISH_BE_RELATED_BY_SETMEAL);
}
```

这一步是检查这些菜品有没有被套餐使用。

比如套餐“商务套餐”里包含了菜品“米饭”和“宫保鸡丁”。

如果直接删除“宫保鸡丁”，套餐里的菜品关系就会出问题。

所以只要查询到有套餐关联，就不允许删除。

异常消息是：

```text
当前菜品关联了套餐,不能删除
```

## 10. SetmealDishMapper

文件位置：

```text
sky-server/src/main/java/com/sky/mapper/SetmealDishMapper.java
```

代码：

```text
@Mapper
public interface SetmealDishMapper {

    List<Long> getSetmealIdsByDishIds(List<Long> dishIds);
}
```

这个 Mapper 操作的是套餐菜品关系表：

```text
setmeal_dish
```

它的作用是根据一批菜品 id 查询关联的套餐 id。

例如传入：

```text
dishIds = [1, 2, 3]
```

如果返回：

```text
[10, 11]
```

说明这些菜品中至少有菜品被套餐 id 为 10 或 11 的套餐关联了。

## 11. SetmealDishMapper.xml

文件位置：

```text
sky-server/src/main/resources/mapper/SetmealDishMapper.xml
```

核心代码：

```text
<select id="getSetmealIdsByDishIds" resultType="java.lang.Long">
    select setmeal_id from setmeal_dish where dish_id in
    <foreach collection="dishIds" item="dishId" separator="," open="(" close=")">
        #{dishId}
    </foreach>
</select>
```

这段 SQL 最终会被拼成：

```text
select setmeal_id from setmeal_dish where dish_id in (1, 2, 3)
```

### 11.1 foreach 的作用

`foreach` 用来遍历集合参数。

这里：

```text
collection="dishIds"
```

表示遍历传进来的菜品 id 列表。

```text
item="dishId"
```

表示每次循环时，当前元素叫 `dishId`。

```text
separator=","
```

表示每个元素之间用逗号分隔。

```text
open="(" close=")"
```

表示最终 SQL 外面包一层小括号。

所以传入：

```text
[1, 2, 3]
```

最终 SQL 是：

```text
in (1, 2, 3)
```

## 12. 第三步：删除菜品和口味

校验都通过后，执行：

```text
for (Long id : ids) {
    dishMapper.deleteById(id);
    dishFlavorMapper.deleteByDishId(id);
}
```

这里删除两类数据。

第一句：

```text
dishMapper.deleteById(id);
```

删除 `dish` 表中的菜品主数据。

第二句：

```text
dishFlavorMapper.deleteByDishId(id);
```

删除 `dish_flavor` 表中这个菜品对应的口味数据。

为什么要删除口味？

因为菜品已经不存在了，口味数据也没有意义了。

如果只删 `dish`，不删 `dish_flavor`，就会留下无效数据：

```text
dish_flavor.dish_id 指向一个已经不存在的菜品 id
```

## 13. DishMapper.deleteById

文件位置：

```text
sky-server/src/main/java/com/sky/mapper/DishMapper.java
```

代码：

```text
@Delete("delete from dish where id = #{id}")
void deleteById(Long id);
```

作用是根据 id 删除 `dish` 表中的一条菜品数据。

例如：

```text
delete from dish where id = 1
```

## 14. DishFlavorMapper.deleteByDishId

文件位置：

```text
sky-server/src/main/java/com/sky/mapper/DishFlavorMapper.java
```

代码：

```text
@Delete("delete from dish_flavor where dish_id = #{dishId}")
void deleteByDishId(Long dishId);
```

作用是根据菜品 id 删除对应的口味数据。

例如：

```text
delete from dish_flavor where dish_id = 1
```

这里删除条件是：

```text
dish_id
```

不是口味表自己的主键：

```text
id
```

因为一个菜品可能对应多条口味记录。

## 15. 异常怎么返回给前端

Service 中抛出的是：

```text
DeletionNotAllowedException
```

这个异常继承自项目里的业务异常基类。

项目里有全局异常处理器：

```text
GlobalExceptionHandler
```

它会捕获业务异常，并返回：

```text
Result.error(ex.getMessage())
```

所以如果删除起售菜品，前端会收到类似：

```text
{
  "code": 0,
  "msg": "起售中的菜品不能删除"
}
```

如果删除被套餐关联的菜品，前端会收到：

```text
{
  "code": 0,
  "msg": "当前菜品关联了套餐,不能删除"
}
```

## 16. 为什么要先校验再删除

删除菜品属于有风险的操作。

如果不校验，可能出现：

```text
1. 删除正在售卖的菜品，影响用户下单
2. 删除套餐里正在使用的菜品，导致套餐数据不完整
3. 删除主表但没删除口味表，留下无效口味数据
```

所以删除业务一般不是简单执行：

```text
delete from dish where id = ?
```

而是：

```text
先判断业务上允许不允许删，再执行数据库删除。
```

## 17. 当前代码的两个注意点

### 17.1 建议先删口味，再删菜品

当前代码是：

```text
dishMapper.deleteById(id);
dishFlavorMapper.deleteByDishId(id);
```

如果数据库里 `dish_flavor.dish_id` 对 `dish.id` 建了外键，并且没有级联删除，那么先删 `dish` 可能会失败。

更常见的顺序是：

```text
dishFlavorMapper.deleteByDishId(id);
dishMapper.deleteById(id);
```

也就是：

```text
先删从表，再删主表。
```

如果当前数据库没有外键约束，现有顺序也能跑；但从数据关系上讲，先删口味会更稳。

### 17.2 List 参数建议加 @Param

`SetmealDishMapper` 现在是：

```text
List<Long> getSetmealIdsByDishIds(List<Long> dishIds);
```

XML 中使用：

```text
collection="dishIds"
```

如果项目编译时保留了参数名，这样可以正常工作。

但有些环境下 MyBatis 可能识别不到 `dishIds` 这个参数名，报类似：

```text
Parameter 'dishIds' not found
```

更稳的写法是：

```text
List<Long> getSetmealIdsByDishIds(@Param("dishIds") List<Long> dishIds);
```

这样 XML 里的：

```text
collection="dishIds"
```

就一定能对应上。

`DishFlavorMapper.insertBatch` 同理，如果 XML 里使用 `collection="flavors"`，也建议接口参数加：

```text
@Param("flavors")
```

## 18. 删除菜品主线记忆法

可以按这条线记：

```text
DishController.delete
-> 接收 ids
-> DishService.deleteBatch
-> 查询每个菜品状态
-> 起售中则禁止删除
-> 查询 setmeal_dish 是否有关联套餐
-> 有套餐关联则禁止删除
-> 删除 dish 数据
-> 删除 dish_flavor 口味数据
-> 返回成功
```

如果只记一句话：

```text
删除菜品 = 校验起售状态 + 校验套餐关联 + 删除菜品主表 + 删除口味从表。
```
